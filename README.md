# CLML-Test-API-Template

CLML - classic ML (nwm)

That's my practice for integrating different formats of models and deploying them to an Inference server (FastAPI and Triton)

(**Template**): Docker command for setup:

```bash
docker compose up -d --build
```
