[project]
name = "webui"
version = "0.1.0"
description = "Streamlit front-end for either serving profile"
requires-python = "==3.12.*"
dependencies = [
    "streamlit==1.60.0",
    "requests==2.34.2",
    "pandas==2.3.3",
]

[tool.uv]
# An application, not a library: uv manages the environment and builds nothing.
package = false
